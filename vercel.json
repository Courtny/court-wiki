{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm db:generate && pnpm --filter @court-wiki/web build",
  "outputDirectory": "apps/web/.next",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}
